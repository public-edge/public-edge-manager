{{- if .Values.enabled }}
apiVersion: v1
kind: ConfigMap
metadata: {name: {{ include "public-edge-manager.name" . }}-records}
data:
  records.json: {{ dict "zones" .Values.dns.zones "externalRecords" .Values.dns.externalRecords "services" .Values.services | toJson | quote }}
{{- end }}
